{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug fivelabsengine",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/fivelabsengine",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cmake: build"
        }
    ]
}
